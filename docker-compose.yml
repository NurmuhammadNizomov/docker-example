services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-spa-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
    environment:
      - NGINX_HOST=${DOMAIN:-localhost}
